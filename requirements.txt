# Healthcare Agentic AI — Production Grade (demo build)
# Core
pydantic==2.*
pydantic-settings==2.*
python-dotenv==1.*
structlog==24.*
httpx==0.27.*

# API
fastapi==0.*
uvicorn[standard]==0.*

# Testing
pytest==8.*
pytest-asyncio==0.23.*

# Notes:
#  - LLM providers (Groq / Gemini / Anthropic) are called over HTTP via httpx,
#    so no vendor SDK is required. Keys come from .env.
#  - Synthea (synthetic patient data) is a separate Java tool used in Phase 2
#    to seed the sandbox EHR; it is not a Python dependency.
